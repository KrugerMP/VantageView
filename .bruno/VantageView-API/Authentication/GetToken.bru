meta {
  name: GetToken
  type: http
  seq: 1
}

post {
  url: http://localhost:5098/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "Username": "admin",
    "Password": "admin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
